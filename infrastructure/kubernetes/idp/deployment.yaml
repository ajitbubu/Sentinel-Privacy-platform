apiVersion: apps/v1
kind: Deployment
metadata:
  name: idp-backend
  namespace: consent-platform
spec:
  replicas: 2
  selector:
    matchLabels: { app: idp-backend }
  template:
    metadata:
      labels: { app: idp-backend }
    spec:
      containers:
        - name: idp-backend
          image: your-registry/idp-backend:latest
          ports: [{ containerPort: 8002 }]
          envFrom:
            - secretRef: { name: platform-secrets }
          readinessProbe:
            httpGet: { path: /api/v1/health, port: 8002 }
            initialDelaySeconds: 5
          resources:
            requests: { cpu: 250m, memory: 256Mi }
            limits: { cpu: "1", memory: 512Mi }
---
apiVersion: v1
kind: Service
metadata:
  name: idp-backend
  namespace: consent-platform
spec:
  selector: { app: idp-backend }
  ports: [{ port: 8002, targetPort: 8002 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: idp-backend
  namespace: consent-platform
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: idp-backend }
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource: { name: cpu, target: { type: Utilization, averageUtilization: 70 } }
